<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Whiz POS</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-gray-50 text-gray-900 flex flex-col min-h-screen">
    <nav class="fixed top-0 left-0 w-full z-50 bg-white/90 backdrop-blur-md border-b border-gray-200 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <div class="flex items-center">
                    <a href="/" class="text-2xl font-bold text-blue-600">Whiz POS</a>
                </div>
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-4">
                        <a href="/" class="px-3 py-2 rounded-md text-sm font-medium hover:text-blue-600 transition-colors">Home</a>
                        <a href="/windows" class="px-3 py-2 rounded-md text-sm font-medium hover:text-blue-600 transition-colors">Windows POS</a>
                        <a href="/mobile" class="px-3 py-2 rounded-md text-sm font-medium hover:text-blue-600 transition-colors">Mobile App</a>
                        <a href="/backoffice" class="px-3 py-2 rounded-md text-sm font-medium hover:text-blue-600 transition-colors">Back Office</a>
                        <% if (user) { %>
                            <span class="text-sm font-medium text-gray-500">Hi, <%= user.contactPerson.split(' ')[0] %></span>
                            <a href="/auth/logout" class="px-3 py-2 rounded-md text-sm font-medium text-red-500 hover:text-red-700">Logout</a>
                        <% } else { %>
                            <a href="/login" class="px-3 py-2 rounded-md text-sm font-medium text-emerald-600 hover:text-emerald-700">Login</a>
                        <% } %>
                    </div>
                </div>
                <div class="-mr-2 flex md:hidden">
                    <button type="button" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')" class="bg-white inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" aria-controls="mobile-menu" aria-expanded="false">
                        <span class="sr-only">Open main menu</span>
                        <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <div class="hidden md:hidden" id="mobile-menu">
            <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3 bg-white border-b border-gray-200 shadow-lg">
                <a href="/" class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50">Home</a>
                <a href="/windows" class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50">Windows POS</a>
                <a href="/mobile" class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50">Mobile App</a>
                <a href="/backoffice" class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-blue-600 hover:bg-gray-50">Back Office</a>
                <% if (user) { %>
                    <div class="pt-4 pb-3 border-t border-gray-200">
                        <div class="flex items-center px-5">
                            <div class="text-base font-medium text-gray-800">Hi, <%= user.contactPerson %></div>
                        </div>
                        <div class="mt-3 space-y-1 px-2">
                            <a href="/auth/logout" class="block px-3 py-2 rounded-md text-base font-medium text-red-600 hover:text-red-800 hover:bg-gray-50">Logout</a>
                        </div>
                    </div>
                <% } else { %>
                    <a href="/login" class="block w-full text-center mt-4 px-5 py-3 rounded-md font-medium text-emerald-600 bg-emerald-50 hover:bg-emerald-100">Login</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main class="flex-grow pt-16">
